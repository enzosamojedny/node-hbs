document.addEventListener("DOMContentLoaded", async function addToCart() {
    //tengo que agregar en payload user.email, product data via req.params con el thumbnail y crear un nuevo cart id, despues 
});
module.exports = { addToCart };
